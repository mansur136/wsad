def is_prime(n):
    if n<2:
        return False
    for i in range(2,int(n**0.5)+1):
        if n%i==0:
            return False
    return True
def filter_prime(numbers):
    primes=[]
    for num in numbers:
        if is_prime(num):
            primes.append(num)
    return primes

input_str=input("Введите числа ")
nums=list(map(int,input_str.split()))
print("Простые числа:",filter_prime(nums))


